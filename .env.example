# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="file:./prisma/dev.db"

# Public URL for generating QR codes and join links
# This should be the externally accessible URL of your application
# Examples:
#   - Local development: http://localhost:3000
#   - Local network: http://192.168.1.100:3000
#   - Production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Default language for the application (optional)
# If not set, defaults to 'en' (English)
# Supported values: 'en' (English), 'de' (German)
DEFAULT_LANG="en"

# Admin passphrase for /host and /admin sections (optional)
# If set, users will be prompted for this passphrase when accessing
# the admin area and host management sections
# The passphrase is hashed with bcrypt (salt rounds: 10) before being stored in cookies
# SECURITY: Never store the cleartext password in cookies or logs
# Joining games is always possible without a password
# Example: ADMIN_PASSPHRASE="my-secret-password"
ADMIN_PASSPHRASE=""
